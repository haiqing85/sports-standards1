name: 自动抓取更新体育标准数据库

on:
  schedule:
    - cron: '0 20 * * 2'  # 每周三 UTC 20:00（北京时间周四 04:00），避开 CNB 周一运行
  push:
    branches: [ main ]
    paths:
      - 'scripts/update_standards.py'
      - 'scripts/fetch_sactc.py'
  workflow_dispatch:
    inputs:
      mode:
        description: '运行模式'
        required: true
        default: 'full'
        type: choice
        options:
          - full
          - check
          - pdf

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: 检出仓库
        uses: actions/checkout@v4

      - name: 配置 Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安装依赖
        run: pip install -r requirements.txt   # 改为从 requirements.txt 安装，含 beautifulsoup4/lxml

      - name: 完整抓取
        if: ${{ github.event.inputs.mode == 'full' || github.event.inputs.mode == '' }}
        run: python scripts/update_standards.py
        timeout-minutes: 120

      - name: 核查标准状态
        if: ${{ github.event.inputs.mode == 'check' }}
        run: python scripts/update_standards.py --check
        timeout-minutes: 20

      - name: 下载缺失 PDF
        if: ${{ github.event.inputs.mode == 'pdf' }}
        run: python scripts/download_pdfs.py
        timeout-minutes: 60

      - name: 提交变更
        run: |
          git config user.name "Standards Bot"
          git config user.email "bot@github-actions.com"
          git add data/standards.json
          git add data/update_log.txt 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "数据无变化，无需提交"
          else
            TODAY=$(date '+%Y-%m-%d')
            git commit -m "🤖 自动更新 $TODAY"
            git pull --rebase origin main   # 推送前先拉取，避免与 CNB 并发推送冲突
            git push
          fi
